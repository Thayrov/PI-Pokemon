import {LoaderContainer, StyledLoader} from '../styles/Loader.styles';

const Loader = () => {
  return (
    <LoaderContainer>
      <StyledLoader />
    </LoaderContainer>
  );
};

export default Loader;
